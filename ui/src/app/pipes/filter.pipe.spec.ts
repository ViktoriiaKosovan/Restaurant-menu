import { FilterPipeCategory } from './filterCategory.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeCategory();
    expect(pipe).toBeTruthy();
  });
});
